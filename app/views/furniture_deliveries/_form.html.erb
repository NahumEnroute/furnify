<%= form_with(model: furniture_delivery) do |form| %>
  <% if furniture_delivery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(furniture_delivery.errors.count, "error") %> prohibited this furniture_delivery from being saved:</h2>

      <ul>
        <% furniture_delivery.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :furniture_id %>
    <%= form.select :furniture_id, @furniture_options, { include_blank: true, selected: @furniture_delivery.furniture_id } %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :product_send %>
    <%= form.check_box :product_send %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.number_field :cost %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
